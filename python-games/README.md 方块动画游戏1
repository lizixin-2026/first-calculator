 方块动画游戏（pygame）

 一、项目背景

我之前用 pygame 做了一个自动反弹的方块。自动反弹那个不用管它，它自己跑；但我更想做一个“我能控制它”的版本，让他更接近一个游戏，而不是单纯的动

于是就写了这个。用方向键控制方块移动，屏幕上实时显示方块坐标，还有一个“高地”区域，走到那里会显示提示文字，让它也有些游戏的任务感。

 二、项目功能

- 打开一个 800×600 的窗口，标题是“手动操控方块动画”
- 用方向键控制青色方块上下左右移动
- 屏幕上实时显示方块当前位置，比如“方块位置:(400,300)”
- 左上角显示操作提示“按方向键移动”
- 方块走到右上角区域（x>700 且 y<100），屏幕中间显示“你到达了高地!”
- 方块不会跑出窗口，到边界就停住
- 每秒最多 60 帧

三、技术方案

- 编程语言：Python 3.13
- 使用库：pygame

四，程序结构

- `import pygame` 导入游戏库，`import sys` 用来退出程序
- `pygame.init()` 初始化 pygame
- `pygame.display.set_mode()` 创建 800×600 窗口
- `pygame.time.Clock()` 提前建好时钟对象，后面控制帧率
- 用 `x, y` 存方块左上角坐标，`speed` 存每次移动的像素数
- 用 `pygame.font.Font()` 加载微软雅黑字体
- 用 `while True` 做主循环，每一帧做这些事：
  1. 处理退出事件
  2. 检测方向键是否按着
  3. 移动方块坐标
  4. 限制方块不出界
  5. 清屏  画方块  画文字
  6. 判断是否到达高地
  7. 刷新屏幕  继续

**2. 怎么限制方块不出界**

x = max(0, min(750, x))
y = max(0, min(550, y))
