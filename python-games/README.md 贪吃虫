 贪吃虫（tkinter）

 一、项目背景

前面做过计算器和方块动画，都是简单的函数堆叠。贪吃蛇是我第一次做一个有状态、有逻辑、有输赢的小游戏。

它让我理解了：一个完整的游戏，不只是“画东西”，还要管数据、管状态、管循环、管结束。

 二、项目功能

- 打开一个 400×400 的窗口，标题是“贪吃蛇”
- 方向键控制蛇的移动
- 吃到红色食物得 10 分，蛇变长
- 撞墙或撞自己游戏结束
- 结束后屏幕中间显示“游戏结束”，按 R 重新开始
- 顶部实时显示当前得分
- 每 200 毫秒移动一次

 三、技术方案

- 编程语言：Python 3.13
- 使用库：tkinter、random
- 开发工具：IDLE

程序结构：
-  `SnakeGame` 把数据和操作打包
- `__init__` 初始化窗口、画布、得分标签，绑定键盘，启动游戏循环
- `reset_game()` 重置蛇的位置、方向、食物、得分
- `random_food()` 随机生成食物，避开蛇身
- `change_direction()` 按键改方向，防止反向
- `move_snake()` 移动蛇、检测碰撞、吃食物
- `draw()` 清屏、画食物、画蛇、画结束文字
- `game_loop()` 用 `after` 定时刷新
- `restart()` 游戏结束后按 R 重置

 四、关键代码思路




