 方块动画（pygame）
 一、项目背景

这是我学 pygame 之后写的第一个动画。方块在窗口里自己移动，碰到边界就反弹，不用管它，它自己一直跑。

 二、项目功能

- 打开一个 1200×800 的窗口，标题是“第一个方块动画”
- 一个青色方块在窗口里自动移动
- 碰到左右边界，横向速度反向；碰到上下边界，纵向速度反向
- 方块一直跑，不点 × 不会停
- 每秒最多 60 帧，速度稳定

 三、技术方案

- 编程语言：Python 3.13
- 使用库：pygame
- 开发工具：IDLE

程序结构：
- `import pygame` 导入游戏库，`import sys` 用来退出程序
- `pygame.init()` 初始化 pygame 的所有模块
- `pygame.display.set_mode()` 创建窗口，`set_caption()` 设置标题
- 用 `x`、`y` 存方块左上角坐标，`speed_x`、`speed_y` 存每帧移动的像素数
- 用 `while True` 做主循环，每一帧做四件事：
  1. 检查有没有关窗口
  2. 移动方块坐标
  3. 判断是否碰壁，碰了就速度取反
  4. 清屏 → 画方块 → 刷新屏幕
- 用 `pygame.time.Clock().tick(60)` 控制帧率

 四、关键代码思路

**1. 动画的原理**

动画就是“坐标不断变化，画面不断重画”。每一帧让 `x = x + speed_x`，方块看起来就在动。

**2. 碰壁反弹**

if x > 1150 or x < 0:
    speed_x = -speed_x
